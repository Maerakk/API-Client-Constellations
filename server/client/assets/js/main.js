import StarGazer from "../../src/StarGazer.js";

document.addEventListener('DOMContentLoaded', function() {
    //side nav init
    var elems = document.querySelectorAll('.sidenav');
    var instances = M.Sidenav.init(elems);

    //autocomplete init

});